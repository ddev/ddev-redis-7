name: "Test DDEV Addon"
on:
  pull_request:
  merge_group:

jobs:
  run_tests:
    name: Run tests
    uses: oblakstudio/workflows/.github/workflows/reusable_test_ddev_addon.yml@v1
    with:
      test_type: "local"
