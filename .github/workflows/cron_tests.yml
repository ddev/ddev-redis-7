name: "Daily tests"
on:
  schedule:
    - cron: "25 08 * * *"

jobs:
  run_tests:
    name: Run tests
    uses: oblakstudio/workflows/.github/workflows/reusable_test_ddev_addon.yml@v1
    with:
      ddev_version: '["stable", "HEAD", "edge"]'
      test_type: "release"
